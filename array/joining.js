// join method return string

const numbers = [1, 2, 3, 4, 5];
const joined = numbers.join(",");
console.log(joined);

const message = "This is my country";
const parts = message.split(" ");
console.log(parts);
