const numbers = [1, 2, 3, 4, 5, 7];

// end
const last = numbers.pop();
//Beginning
const first = numbers.shift();
//Middle
numbers.splice(2, 1); // first count holo koto number and then second count hoilo koto ghula dlete hobe
console.log(numbers);
